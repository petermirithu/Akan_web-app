



function validate() {
  var date= parseInt(document.forms["akan"]["date"] .value);
  var month=parseInt(document.forms["akan"][month] .value);
  var year=parseInt(document.forms["akan"]["date"] .value);
  var result=date, month, year ;
  alert(result);
}